mysql1:
  user: root
  pwd: admin123456
  # Take care:
  # Bridge network mode, can not be localhost!
  # The IP can be change if the container restart 
  ip: mysql
  port: 3306
  dbname: test
  maxOpenConns: 25
  maxIdleConns: 25

redis1:
  user: root
  # Take care:
  # Bridge network mode, can not be localhost!
  # The IP can be change if the container restart 
  host: redis
  port: 6379
  database: 0
  netowork: tcp
  poolSize: 12

tika:
  host: tika
  port: 9998

service:
  port: 8080